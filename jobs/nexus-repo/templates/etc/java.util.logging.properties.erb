# Empty java.util.logging.properties to prevent the log to stderr, so that
# all logs will be delegated to pax logging JUL handler only
<%- p('nexus_repo.java_util_logging_properties', {}).each do |key, val| -%>
  <%= key %> = <%= val %>
<%- end -%>